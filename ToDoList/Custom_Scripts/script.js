$(document).ready(function () {

    $(':checkbox[name=vehicle]').next('label').text();
    // adauga clsa completed cand apasam pe el
    //$("ul").on("click", "li", function () {
    //    $(this).toggleClass("IsCompleted");
    //});

    // raspunde pentru stergere, adica apasarea butonului delete
    //$("ul").on("click", "span", function (event) {
    //    $(this).parent().fadeOut(500, function () {
    //        $(this).remove();
    //    });
    //    event.stopPropagation();
    //});

    //$("input[type='text']").keypress(function (event) {
    //    if (event.which === 13) {

    //        var todoText = $(this).val();
    //        $(this).val("");

    //        $("ul").append("<li><span><i class='fa fa-trash'></i></span> " + ////Text + "</l//")
    //    }
    //});

   ///$(".fa-plus").click(function () {
   //    ("input[type='text']").fadeToggle();
   // });
});